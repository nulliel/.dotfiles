# todo: n
# todo: node
# todo: emacs
# todo: cron / scripts
# todo: zsh

- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.aliases: aliases
    ~/.antigen.zsh: antigen/antigen.zsh
    ~/.zshrc: zshrc
    ~/.emacs.d: emacs.d
    ~/.npmrc: npmrc
    ~/.gitconfig: gitconfig
    ~/.gitignore: gitignore

- shell:
  - git submodule update --init --recursive

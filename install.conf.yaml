# todo: n
# todo: node
# todo: emacs
# todo: cron / scripts

- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.bashrc: bashrc
    ~/.emacs.d: emacs.d
    ~/.npmrc: npmrc
    ~/.gitconfig: gitconfig
    ~/.gitignore: gitignore
